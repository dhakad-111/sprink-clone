spring:
  datasource:
    username: sprink
    url: jdbc:h2:mem:sprink-data
    driverClassName: org.h2.Driver
    password: sprink
  h2:
    console:
      enabled: 'true'
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: 'true'
    hibernate:
      ddl-auto: update
server:
  port: '9443'